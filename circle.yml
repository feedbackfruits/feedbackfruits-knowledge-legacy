machine:
  node:
    version: 6.0.0
test:
  post:
    - npm run coverage
deployment:
  production:
    tag: /v[0-9]+(\.[0-9]+)*/
    heroku:
      appname: feedbackfruits-knowledge
  staging:
    branch: master
    heroku:
      appname: staging-fbf-knowledge
